import { FC } from 'react';
import { MemberCreateForm } from './MemberCreateForm';

export default { component: MemberCreateForm };

export const memberCreateForm: FC = () => {
  return <MemberCreateForm />;
};
